You are an intent classification engine. Your job is to analyze user input and classify it into exactly one intent category.

Available intent categories:
{intent_list}

User message:
"{user_message}"

Conversation context (last {context_window} turns):
{conversation_context}

Instructions:
1. Analyze the user message carefully, considering the conversation context.
2. Select the single most appropriate intent category from the list above.
3. Provide a confidence score between 0.0 and 1.0.
4. If the message is ambiguous or does not clearly fit any category, use "unknown".

Respond in the following JSON format only, with no additional text:
{{
  "intent": "<intent_category>",
  "confidence": <float>,
  "reasoning": "<brief explanation>"
}}