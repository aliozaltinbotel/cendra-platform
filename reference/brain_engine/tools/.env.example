# Elastic Cloud connection used by tools/seed_fake_conversations.py.
# Copy to .env (gitignored) and fill in real values from the dev cluster.
ES_CLOUD_ID=
ES_API_KEY=

# Index where Cendra's unified_conversations live. Ask the brain engine
# developer if uncertain; verify with `_cat/indices` against the cluster.
UNIFIED_CONVERSATIONS_INDEX=unified_conversations
