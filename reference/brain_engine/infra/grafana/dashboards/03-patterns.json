{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "PatternRules emitted / hour",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 9, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [
        {
          "expr": "sum by (kind) (rate(brain_patterns_rules_emitted_total[10m])) * 3600",
          "legendFormat": "{{kind}}",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Synthesis acceptance rate (%)",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 9, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {
          "expr": "(1 - sum(rate(brain_patterns_synthesis_rejects_total[10m])) / clamp_min(sum(rate(brain_patterns_synthesis_attempts_total[10m])), 0.0001)) * 100",
          "legendFormat": "accepted",
          "refId": "A"
        }
      ],
      "fieldConfig": {"defaults": {"unit": "percent", "max": 100, "min": 0}, "overrides": []}
    },
    {
      "title": "Cases ingested / minute by scenario",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 9, "w": 12, "x": 0, "y": 9},
      "id": 3,
      "targets": [
        {
          "expr": "sum by (scenario) (rate(brain_patterns_cases_ingested_total[1m])) * 60",
          "legendFormat": "{{scenario}}",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Refusal signals / minute by language",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 9, "w": 12, "x": 12, "y": 9},
      "id": 4,
      "targets": [
        {
          "expr": "sum by (language) (rate(brain_refusal_signals_total[1m])) * 60",
          "legendFormat": "{{language}}",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Refusal signals total by type",
      "type": "stat",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "gridPos": {"h": 6, "w": 24, "x": 0, "y": 18},
      "id": 5,
      "targets": [
        {
          "expr": "sum by (type) (brain_refusal_signals_total)",
          "legendFormat": "{{type}}",
          "refId": "A"
        }
      ],
      "options": {"reduceOptions": {"calcs": ["lastNotNull"]}}
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["brain-engine", "patterns"],
  "templating": {"list": []},
  "time": {"from": "now-3h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "Brain Engine — Patterns",
  "uid": "brain-patterns",
  "version": 1,
  "weekStart": ""
}
